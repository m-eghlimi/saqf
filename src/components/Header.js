import React from 'react';

const Header = () => {
    return (
        <header>
            <div className="logo">SAQF</div>
            <div className="profile">
                <button>Profile</button>
            </div>
        </header>
    );
};

export default Header;